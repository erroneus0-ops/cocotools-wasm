name: Build tocgen WASM

on:
  workflow_dispatch:
  push:
    paths:
      - 'wasm_builds/tocgen/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mymindstorm/setup-emsdk@v14
        with:
          version: latest
      - name: Build
        continue-on-error: true
        id: build_step
        run: |
          set -o pipefail
          cd wasm_builds/tocgen
          bash build.sh 2>&1 | tee /tmp/tocgen_build_output.txt
      - name: Write build report
        if: always()
        run: |
          echo "# tocgen WASM Build Report" > wasm_builds/tocgen/BUILD_REPORT.md
          echo "**Built:** $(date -u '+%Y-%m-%d %H:%M UTC')" >> wasm_builds/tocgen/BUILD_REPORT.md
          echo "**Size:** $(wc -c < wasm_builds/tocgen/tocgen.wasm 2>/dev/null || echo 'build failed') bytes" >> wasm_builds/tocgen/BUILD_REPORT.md
          if [ "${{ steps.build_step.outcome }}" != "success" ]; then
            echo "" >> wasm_builds/tocgen/BUILD_REPORT.md
            echo "## Build FAILED -- full output below" >> wasm_builds/tocgen/BUILD_REPORT.md
            echo '```' >> wasm_builds/tocgen/BUILD_REPORT.md
            cat /tmp/tocgen_build_output.txt >> wasm_builds/tocgen/BUILD_REPORT.md 2>/dev/null || echo "no build output captured" >> wasm_builds/tocgen/BUILD_REPORT.md
            echo '```' >> wasm_builds/tocgen/BUILD_REPORT.md
          fi
      - name: Commit
        if: always()
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add wasm_builds/tocgen/
          git diff --staged --quiet || git commit -m "build: tocgen WASM [ci skip]"
          git pull --rebase origin main
          git push
      - name: Fail the job if the build actually failed
        if: steps.build_step.outcome != 'success'
        run: exit 1
