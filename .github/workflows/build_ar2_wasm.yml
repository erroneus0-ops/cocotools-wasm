name: Build ar2 WASM

on:
  workflow_dispatch:
  push:
    paths:
      - 'wasm_builds/ar2/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mymindstorm/setup-emsdk@v14
        with:
          version: latest
      - name: Build
        run: |
          set -o pipefail
          cd wasm_builds/ar2
          bash build.sh 2>&1 | tee /tmp/ar2_build_output.log
        continue-on-error: true
        id: build_step
      - name: Write build report
        if: always()
        run: |
          echo "# ar2 WASM Build Report" > wasm_builds/ar2/BUILD_REPORT.md
          echo "**Built:** $(date -u '+%Y-%m-%d %H:%M UTC')" >> wasm_builds/ar2/BUILD_REPORT.md
          echo "**Size:** $(wc -c < wasm_builds/ar2/ar2.wasm 2>/dev/null || echo 'build failed') bytes" >> wasm_builds/ar2/BUILD_REPORT.md
          if [ "${{ steps.build_step.outcome }}" != "success" ]; then
            echo "" >> wasm_builds/ar2/BUILD_REPORT.md
            echo "## Build FAILED -- full output below" >> wasm_builds/ar2/BUILD_REPORT.md
            echo '```' >> wasm_builds/ar2/BUILD_REPORT.md
            cat /tmp/ar2_build_output.log >> wasm_builds/ar2/BUILD_REPORT.md
            echo '```' >> wasm_builds/ar2/BUILD_REPORT.md
          fi
      - name: Commit
        if: always()
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add wasm_builds/ar2/
          git diff --staged --quiet || git commit -m "build: ar2 WASM [ci skip]"
          git pull --rebase origin main
          git push
      - name: Fail the job if the build actually failed
        if: steps.build_step.outcome != 'success'
        run: exit 1
