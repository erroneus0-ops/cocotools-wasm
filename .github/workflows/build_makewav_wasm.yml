name: Build makewav WASM

on:
  workflow_dispatch:
  push:
    paths:
      - 'wasm_builds/makewav/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mymindstorm/setup-emsdk@v14
        with:
          version: latest
      - name: Build
        run: |
          cd wasm_builds/makewav
          bash build.sh
      - name: Write build report
        if: always()
        run: |
          echo "# makewav WASM Build Report" > wasm_builds/makewav/BUILD_REPORT.md
          echo "**Built:** $(date -u '+%Y-%m-%d %H:%M UTC')" >> wasm_builds/makewav/BUILD_REPORT.md
          echo "**Size:** $(wc -c < wasm_builds/makewav/makewav.wasm 2>/dev/null || echo 'build failed') bytes" >> wasm_builds/makewav/BUILD_REPORT.md
      - name: Commit
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add wasm_builds/makewav/
          git diff --staged --quiet || git commit -m "build: makewav WASM [ci skip]"
          git pull --rebase origin main
          git push
